name: ACT4 architectural regression

on:
  push:
    paths:
      - "rtl/**"
      - "files/core.f"
      - "tb/integration/tb_baremetal.sv"
      - "tools/elf_to_memh.py"
      - "verification/act4/**"
      - "Makefile"
      - ".github/workflows/act4-regression.yml"
  pull_request:
    paths:
      - "rtl/**"
      - "files/core.f"
      - "tb/integration/tb_baremetal.sv"
      - "tools/elf_to_memh.py"
      - "verification/act4/**"
      - "Makefile"
      - ".github/workflows/act4-regression.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: act4-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rv32im:
    name: ACT4 4.0.0 RV32I/RV32M
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install host, RTL, and RISC-V tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            curl \
            unzip \
            verilator

      - name: Install pinned GCC 15 RISC-V toolchain
        env:
          TOOLCHAIN_URL: https://github.com/xpack-dev-tools/riscv-none-elf-gcc-xpack/releases/download/v15.2.0-1/xpack-riscv-none-elf-gcc-15.2.0-1-linux-x64.tar.gz
          TOOLCHAIN_SHA256: aaaa8060c914851a3e5ee1ba82cc3d6f80972f90638a05c6e823a37557a33758
        run: |
          mkdir -p "${RUNNER_TEMP}/riscv-toolchain" \
            "${RUNNER_TEMP}/riscv-toolchain-aliases"
          curl --fail --location --retry 3 --retry-all-errors \
            "${TOOLCHAIN_URL}" \
            -o "${RUNNER_TEMP}/riscv-toolchain.tar.gz"
          echo "${TOOLCHAIN_SHA256}  ${RUNNER_TEMP}/riscv-toolchain.tar.gz" \
            | sha256sum -c -
          tar xzf "${RUNNER_TEMP}/riscv-toolchain.tar.gz" \
            --directory="${RUNNER_TEMP}/riscv-toolchain" \
            --strip-components=1
          ln -s "${RUNNER_TEMP}/riscv-toolchain/bin/riscv-none-elf-gcc" \
            "${RUNNER_TEMP}/riscv-toolchain-aliases/riscv64-unknown-elf-gcc"
          ln -s "${RUNNER_TEMP}/riscv-toolchain/bin/riscv-none-elf-objdump" \
            "${RUNNER_TEMP}/riscv-toolchain-aliases/riscv64-unknown-elf-objdump"
          echo "${RUNNER_TEMP}/riscv-toolchain-aliases" >> "${GITHUB_PATH}"
          echo "${RUNNER_TEMP}/riscv-toolchain/bin" >> "${GITHUB_PATH}"

      - name: Install mise
        uses: jdx/mise-action@v4
        with:
          install: false
          cache: true

      - name: Install pinned Sail reference model
        run: |
          mkdir -p "${RUNNER_TEMP}/sail-riscv"
          curl --fail --location --retry 3 --retry-all-errors \
            https://github.com/riscv/sail-riscv/releases/download/0.10/sail-riscv-Linux-x86_64.tar.gz \
            -o "${RUNNER_TEMP}/sail-riscv.tar.gz"
          echo "cb66af6efb5d1ebb812cf19ca2156c2b1020881f08f16c40136da7e84af299ca  ${RUNNER_TEMP}/sail-riscv.tar.gz" \
            | sha256sum -c -
          tar xzf "${RUNNER_TEMP}/sail-riscv.tar.gz" \
            --directory="${RUNNER_TEMP}/sail-riscv" \
            --strip-components=1
          echo "${RUNNER_TEMP}/sail-riscv/bin" >> "${GITHUB_PATH}"

      - name: Show sign-off tool versions
        run: |
          verilator --version
          riscv64-unknown-elf-gcc --version | head -n 1
          sail_riscv_sim --version
          mise --version

      - name: Generate and run ACT4 on RTL
        run: make act4

      - name: Upload ACT4 failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: act4-failure-diagnostics
          if-no-files-found: ignore
          retention-days: 14
          path: |
            /tmp/rv32im-core-act4-*/rtl-artifacts/
            /tmp/rv32im-core-act4-*/work/rv32im-5stage/elfs/
            /tmp/rv32im-core-act4-*/work/rv32im-5stage/build/**/*.sig.log
            /tmp/rv32im-core-act4-*/work/rv32im-5stage/build/**/*.sig.trace
