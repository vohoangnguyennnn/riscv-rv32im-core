#ifndef RV32IM_FREERTOS_STDLIB_H
#define RV32IM_FREERTOS_STDLIB_H

/*
 * FreeRTOS includes <stdlib.h> for the standard size and null definitions.
 * This port uses static allocation only and deliberately links without libc.
 */
#include <stddef.h>

#endif
